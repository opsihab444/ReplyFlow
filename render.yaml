services:
  - type: web
    name: replyflow
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATA_DIR
        value: /tmp/data
      - key: MONGODB_URI
        sync: false
